"use server";import {revalidatePath} from "next/cache";import {redirect} from "next/navigation";import {formText,friendlyError,requireStore} from "@/lib/current-store";
export async function createCustomer(fd:FormData){const {supabase,store}=await requireStore();const {error}=await supabase.from("customers").insert({store_id:store.id,name:formText(fd,"name"),phone:formText(fd,"phone")||null,address:formText(fd,"address")||null,reference:formText(fd,"reference")||null});if(error)redirect(`/dashboard/clientes?erro=${encodeURIComponent(friendlyError(error.message))}`);revalidatePath("/dashboard/clientes");redirect("/dashboard/clientes?sucesso=Cliente cadastrado.");}
