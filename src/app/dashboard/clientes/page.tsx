import {requireStore} from "@/lib/current-store";import {createCustomer} from "./actions";
export default async function Page({searchParams}:{searchParams:Promise<{erro?:string;sucesso?:string}>}){const p=await searchParams;const {supabase,store}=await requireStore();const {data:customers}=await supabase.from("customers").select("id,name,phone,address,reference,created_at").eq("store_id",store.id).order("name");return <div className="page"><header className="page-head"><div><span>CRM</span><h1>Clientes</h1><p>Cadastro rápido para pedidos e delivery.</p></div></header>{p.erro?<div className="flash error">{p.erro}</div>:null}{p.sucesso?<div className="flash">{p.sucesso}</div>:null}<section className="ops-grid"><article className="surface"><table className="data-table"><thead><tr><th>Cliente</th><th>Telefone</th><th>Endereço</th></tr></thead><tbody>{customers?.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td>{c.phone||"—"}</td><td>{c.address||"—"}<small>{c.reference?` · ${c.reference}`:""}</small></td></tr>)}</tbody></table>{!customers?.length?<p className="empty">Nenhum cliente cadastrado.</p>:null}</article><aside className="surface"><h2>Novo cliente</h2><form action={createCustomer} className="compact-form"><label>Nome<input name="name" required/></label><label>Telefone<input name="phone"/></label><label>Endereço<input name="address"/></label><label>Referência<input name="reference"/></label><button className="button primary">Salvar cliente</button></form></aside></section></div>}
