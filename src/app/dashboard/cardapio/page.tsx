import {QRCode} from "@/components/qr-code";import {createClient} from "@/lib/supabase/server";
export default async function Page(){const s=await createClient();const {data:m}=await s.from("store_members").select("store_id").limit(1).maybeSingle();const {data:store}=m?await s.from("stores").select("name,slug").eq("id",m.store_id).single():{data:null};const base=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";return <div className="page"><header className="page-head"><div><span>CARDÁPIO DIGITAL</span><h1>Produtos e QR Code</h1><p>O mesmo cadastro alimenta a venda interna e o menu público.</p></div><button className="button primary">+ Novo produto</button></header><section className="dashboard-grid"><article className="surface"><div className="empty"><h3>Seu cardápio começa aqui</h3><p>Cadastre categorias, produtos, fotos, preços e adicionais.</p></div></article><article className="surface qr-card"><h2>QR Code permanente</h2>{store?<><QRCode value={`${base}/cardapio/${store.slug}`}/><a href={`/cardapio/${store.slug}`} target="_blank">Abrir cardápio público</a></>:<p>Salve primeiro a identidade da loja em Configurações.</p>}</article></section></div>}
