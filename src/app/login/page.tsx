import Link from "next/link";import {login,signup} from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{erro?:string}>}){const {erro}=await searchParams;return <main className="auth-page"><section className="auth-card"><Link href="/" className="brand"><span>CF</span><strong>Controle Fácil</strong></Link><div><h1>Acesse sua operação</h1><p>Entre ou crie a primeira conta administrativa.</p></div>{erro?<div className="alert">{erro}</div>:null}<form><label>Nome completo<input name="name" placeholder="Seu nome"/></label><label>E-mail<input name="email" type="email" required placeholder="voce@loja.com"/></label><label>Senha<input name="password" type="password" minLength={6} required placeholder="Mínimo de 6 caracteres"/></label><div className="form-actions"><button className="button primary" formAction={login}>Entrar</button><button className="button ghost" formAction={signup}>Criar conta</button></div></form><small>Ao criar a conta, confirme o e-mail caso solicitado.</small></section></main>}
